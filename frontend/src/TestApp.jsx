// Minimal test — no imports from recharts/leaflet/socket.io
const TestApp = () => (
  <div style={{ background: 'red', color: 'white', padding: '40px', fontSize: '2rem' }}>
    ✅ React is mounting! The issue is in a component import.
  </div>
);
export default TestApp;
